name: Audit

on:
  push:
  pull_request:
    types:
      - opened
jobs:
  test:
    name: 'Pull Request'
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: |-
          rm -rf node_modules
          yarn install
          yarn audit

      - name: Test
        run: echo Testing

      # - name: Audit
      #   run: |-
      #     yarn audit
